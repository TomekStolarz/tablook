import { ElementVisibleDirective } from './element-visible.directive';

describe('ElementVisibleDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementVisibleDirective();
    expect(directive).toBeTruthy();
  });
});
